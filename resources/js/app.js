// resources/js/app.js

// Default Laravel bootstrap (handles axios, csrf, etc.)
import './bootstrap';

// Import Preline components (for modals, dropdowns, etc.)
import 'preline';

// (Optional) — If you ever re-enable Vue later, uncomment this block:
// import { createApp } from 'vue';
// import { createHeadlessUiPlugin } from '@headlessui/vue';
// import App from './App.vue';

// const app = createApp(App);
// app.use(createHeadlessUiPlugin());
// app.mount('#app');
